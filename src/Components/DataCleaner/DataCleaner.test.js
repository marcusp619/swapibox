import * as DataCleaner from './DataCleaner';


describe('DataCleaner', () => {
  // beforeEach(() => {
  //   window.fetch = jest.fn().mockImplementation(() => ({
  //     status: 200,
  //     json: () => Promise.resolve({})
  //   }));
  // })

  describe('cleanFilmData', async() => {
    it.skip('should call fetch', () => {
    })
  })
})